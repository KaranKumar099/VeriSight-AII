PORT=4000
CLIENT_URL=http://localhost:5174
SIMULATOR_AUTOSTART=true
SEED_DEMO=true

# Optional production database. If omitted, the app uses a local JSON demo store.
# DATABASE_URL=postgres://exam_monitor:exam_monitor@localhost:5432/exam_monitor
